{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Docker Browser 默认能力：所有窗口可监听事件、调用 dialog/opener 插件命令。",
  "windows": ["*"],
  "permissions": ["core:default", "dialog:default", "opener:default"]
}
